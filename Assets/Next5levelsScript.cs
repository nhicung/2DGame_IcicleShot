using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class Next5levelsScript : MonoBehaviour {
    
    
    void Start()
    {
        
    }
    

    public void moveTheScene()
    {
        /*
        Vector3 pos = CameraScript.pos;
        pos.x += 100;
        pos.y += 100;
        CameraScript.pos = pos;
        */

        //CameraScript.Update();
        //float step = 10;//find a proper value for this
        //Camera.main.gameObject.transform.Translate(new Vector3(step, 0, 0));

            //CameraScript.cameraTransform

        /*
        
        Button b = gameObject.GetComponent<Button>();
        b.onClick.AddListener(
            () =>
            {
                Camera.main.gameObject.transform.Translate(step, 0, 0);
            }
        );
        */
    }
}
